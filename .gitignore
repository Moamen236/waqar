*.log
.DS_Store
.env
.env.backup
.env.production
.phpactor.json
.phpunit.result.cache
/.fleet
/.idea
/.nova
/.phpunit.cache
/.vscode
/.zed
/auth.json
/node_modules
/public/build
/public/hot
/public/storage
/storage/*.key
/storage/pail
/vendor
Homestead.json
Homestead.yaml
Thumbs.db

# Vendor template downloads — not redistributed with this repo.
# The parts the app actually needs are already vendored into the build:
# Larkon's compiled theme in public/admin-theme/, Anvogue's compiled theme in
# resources/css/anvogue/. These two folders are the raw, licensed downloads
# they came from and are only needed when porting another page from them.
/Admin Template
/Frontend template

# Claude Code runtime state (the skills under .claude/skills are committed).
/.claude/scheduled_tasks.lock
/.claude/settings.local.json
